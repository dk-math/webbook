const chai = require("chai");
const expect = chai.expect;

describe("good", () => {
  it("initial good count is zero", () => {
    expect(1).to.equal(1);
  });
  
  it("when click, add good counts", () => {
    expect(1).to.equal(1);
  });
});